<h1>Welcome to MyQuote</h1>
<p>Inspirational quotes to feed your soul!</p>
<h3>Recently Added Quotes</h3>
<hr />
<% @quotes.each do |quote| %>
  <div class="card bg-body-tertiary" style="margin-bottom: 10px">
    <div class="card-body">
      <h4 class="card-title">"<%= quote.qtext %>"</h4>
      <p class="card-text"><%= "by #{quote.source.fname} #{quote.source.lname}" %><br />[<a href="/quotes/<%= quote.id %>">Read full quote</a>]</p>
    </div>
  </div>
<% end %>